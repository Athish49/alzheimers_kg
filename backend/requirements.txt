# -----------------------------------------------------------------------------
# backend/requirements.txt
#
# Minimal runtime dependencies for the Alzheimer's KG backend.
# Only packages actually imported by code under backend/ are listed.
# Use loose (>=, <) pins so pip can resolve transitive deps cleanly.
#
# Install:
#     python -m venv venv && source venv/bin/activate
#     pip install -r backend/requirements.txt
# -----------------------------------------------------------------------------

# --- Graph-RAG API ---------------------------------------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.6,<3.0

# --- Neo4j driver ----------------------------------------------------------
neo4j>=5.20,<7.0

# --- HTTP / LLM clients ----------------------------------------------------
requests>=2.31,<3.0
python-dotenv>=1.0,<2.0

# Optional LLM providers (imported lazily in graph_rag/llm_client.py).
# Keep installed so all providers work out-of-the-box.
openai>=1.30,<2.0
anthropic>=0.25,<1.0

# --- Data processing -------------------------------------------------------
numpy>=1.26,<3.0
pandas>=2.1,<3.0

# --- Scraping / ingestion --------------------------------------------------
beautifulsoup4>=4.12,<5.0
lxml>=5.0,<7.0          # fast parser backend for BeautifulSoup
tqdm>=4.66,<5.0

# --- Ontology processing ---------------------------------------------------
owlready2>=0.46,<1.0
rdflib>=7.0,<8.0
